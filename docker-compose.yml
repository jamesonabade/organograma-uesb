version: '3.8'

services:
  arvore-unidades:
    build: .
    container_name: arvore-unidades-app
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
